import 'package:flutter/material.dart';

@immutable
class AppColors {
  // App Colors
  static const primaryColor = Color(0xFF008B99);
  static const textButtonColor = Color(0xFF545454);

  // Transparent
  static const transparent = Colors.transparent;

  // White
  static const white = Color(0xFFFFFFFF);

  // Black
  static const black = Color(0xFF000000);

  // Gray
  static const grey = Color(0xFF707070);
  static const darkSlateGrey = Color(0xFF454B60);

  // Oranges
  static const darkOrange = Color(0xFFF69400);

  const AppColors._();
}
