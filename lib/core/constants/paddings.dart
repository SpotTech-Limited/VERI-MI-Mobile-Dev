import 'package:flutter/material.dart';

@immutable
class Paddings {
  static const defaultPadding = EdgeInsets.symmetric(
    horizontal: 24.0,
    vertical: 36.0,
  );

  static const largerPadding = EdgeInsets.all(48);

  Paddings._();
}
